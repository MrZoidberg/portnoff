# Dashdevs project page
At first please install all dependencies:

```
npm install
```

Then you can use gulp to watch and build project.

Type 
```
gulp default
```
for development and

```
gulp build

```
just for project build.